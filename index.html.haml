---
layout: base
title: Blog
---

- for post in page.posts
  %div{:class=>'post-preview'}
    .post
      %header
        %h2.title{:id=>'post-title'}
          %a{:href=>post.url}
            = post.title
      .content
        .date-box
          .date-month
            = post.date.strftime( '%b' )
          .date-day
            - if post.date.strftime( '%d' )[0,1] == "0"
              = post.date.strftime( '%d' )[1,2]
            - else
              = post.date.strftime( '%d' )
          .date-year
            = post.date.strftime( '%Y' )
        %img{:src=>post.previewimage, :style=>'margin: 0 10px 0 0', :width=>'50px', :align=>'left'}
        ~post.preview
        %a{:href=>post.url}
          [read more]
      %h4.tags.clear
        - for tag in post.tags
          - unless tag.nil?
            %a{:href=>tag.primary_page.url}
              = tag
            |
      .comments
        = post.intense_debate_comments_link

= page.posts.links
